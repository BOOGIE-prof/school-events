# Локально приложение работает на SQLite и не требует ничего сверх стандартной библиотеки.
# Драйвер нужен, когда задана переменная DATABASE_URL (PostgreSQL на сервере).
psycopg2-binary>=2.9,<3
# Push-уведомления: нужен только когда заданы ключи VAPID.
pywebpush>=2.0,<3
