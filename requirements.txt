# Локально приложение работает на SQLite и не требует ничего сверх стандартной библиотеки.
# Драйвер ниже нужен только когда задана переменная DATABASE_URL (PostgreSQL на сервере).
psycopg2-binary>=2.9,<3
